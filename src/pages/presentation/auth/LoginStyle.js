import { Box, styled } from "@mui/material";

export const ContainerWrappper = styled(Box)`
  display: flex;
  flex-direction:column;
  width: auto;
  justify-content: center;
  align-items: center;
  position: fixed;
  top: 0;
  margin-top: 60px;

`;
